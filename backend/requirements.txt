# Installation:
#   uv pip install -r requirements.txt
#   (or: pip install -r requirements.txt)
# =============================================================================

# ── 1. Web Framework & API Server ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4
pydantic-settings==2.3.0
python-multipart==0.0.9
email-validator>=2.1.0

# ── 2. HTTP Clients & Cloud Services ─────────────────────────────────────────
httpx==0.27.0
boto3>=1.34.0

# ── 3. Database & Object Storage ─────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg[binary]>=3.1.18          # PostgreSQL async driver for Supavisor transaction pooler
asyncpg>=0.29.0                  # Secondary async postgres driver
greenlet>=3.0.0                  # Async SQLAlchemy requirement
alembic==1.13.1                  # Database schema migrations
pgvector>=0.2.5                  # 512-d biometric vector similarity search
aiosqlite>=0.20.0                # Edge offline SQLite cache
minio>=7.2.7                     # Object storage fallback

# ── 4. Authentication, Security & Cryptography ───────────────────────────────
python-jose[cryptography]==3.3.0 # JWT auth tokens
passlib[bcrypt]==1.7.4           # Password hashing
bcrypt==4.1.3                    # Encryption backend
cryptography>=42.0.7             # SHA-256 audit hash chaining & AES-256

# ── 5. Environment, Config & Logging ─────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.1.0
PyYAML>=6.0.1                    # Document validation rules engine

# ── 6. LangGraph StateGraph Orchestration ────────────────────────────────────
langgraph>=1.2.11
langchain-core>=1.6.1

# ── 7. OCR & Multimodal Document Processing ──────────────────────────────────
easyocr>=1.7.1
Pillow>=10.3.0
pillow-heif>=1.0.0
google-generativeai>=0.5.4       # Gemini Flash multimodal vision fallback
passporteye>=2.2.2               # Passport MRZ extraction fallback
pymupdf>=1.24.0                  # Fast PDF parsing & rasterization for document OCR


# ── 8. Computer Vision & Forensic Tampering Detection ────────────────────────
opencv-python>=4.9.0
exifread>=3.0.0
numpy>=1.26.4
scikit-image>=0.23.2
scipy>=1.12.0

# ── 9. Biometrics & Facial Verification (Local & Cloud) ──────────────────────
insightface>=1.0.1               # RetinaFace detector & ArcFace W600K 512-d embedder
onnxruntime>=1.18.0              # CPU/MPS neural inference runtime for InsightFace
onnx>=1.16.0
mediapipe>=0.10.9                # Liveness & anti-spoofing detection
deepface>=0.0.93                 # Secondary multi-model ensemble voter
tf-keras>=2.16.0

# ── 10. Cross-Checkpoint Graph Analytics ─────────────────────────────────────
networkx>=3.2                    # Multi-identity cluster and velocity graph analysis

# ── 11. Deep Learning Framework Runtime ──────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0

# ── 12. Automated Testing Suite ──────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
anyio>=4.3.0
