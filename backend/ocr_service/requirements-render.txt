# ============================================================
# TriPort OCR Service — Render Deployment (Lightweight Mode)
# Optimized for Render Free Tier (512 MB RAM limit)
# ============================================================

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4
pydantic-settings==2.3.0
python-multipart==0.0.9

# HTTP Client
httpx==0.27.0

# Document & Image Processing (PDF rendering & downscaling)
opencv-python-headless>=4.9.0.80

# Document & Image Processing (PDF rendering & downscaling)
Pillow>=10.3.0
PyMuPDF>=1.24.0
numpy>=1.26.4

# Environment & Logging
python-dotenv==1.0.1
structlog==24.1.0
