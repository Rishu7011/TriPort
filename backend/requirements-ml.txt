# ============================================================
# Phase 1+ — ML / AI Dependencies
# Requires Python 3.11 (not 3.12+)
# Install with: uv pip install -r requirements-ml.txt
#
# IMPORTANT: Create venv with Python 3.11 first:
#   uv venv --python 3.11
#   source .venv/bin/activate
#   uv pip install -r requirements-core.txt
#   uv pip install -r requirements-ml.txt
# ============================================================

# ── OCR (Phase 1) ─────────────────────────────────────────
# Note: paddlepaddle only supports Python ≤ 3.11
# On macOS Apple Silicon, install via:
#   pip install paddlepaddle==2.6.1 -f https://www.paddlepaddle.org.cn/whl/mac/cpu/stable.html
paddleocr==2.7.3
passporteye==2.2.1             # MRZ parsing + ICAO checksum
Pillow==10.3.0
anthropic==0.26.0              # Claude LLM fallback

# ── Tampering Detection (Phase 2) ────────────────────────
opencv-python-headless==4.9.0.80
exifread==3.0.0
numpy==1.26.4
scikit-image==0.23.2

# ── Face Verification (Phase 2) ──────────────────────────
deepface==0.0.93
tf-keras==2.16.0

# ── CNN Classifier — stretch goal (Phase 2) ──────────────
# torch==2.3.0
# torchvision==0.18.0
# efficientnet-pytorch==0.7.1
